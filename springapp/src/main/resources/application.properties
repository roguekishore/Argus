spring.application.name=springapp

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/springdb?createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

server.port=8080

# ============================================
# Gemini AI Configuration
# ============================================
# Replace with your actual Gemini API key
gemini.api.key=${API_SECRET_KEY}

# # Access token expiration time in milliseconds (24 hours = 86400000)
# jwt.expiration=86400000

# # Refresh token expiration time in milliseconds (7 days = 604800000)
# jwt.refresh-expiration=604800000

# # ============================================
# # Data Initialization Configuration
# # ============================================
# # Enable/disable data initialization on startup
# app.data.initialize=true

# # Default password for initialized users (change in production!)
# app.data.default-password=Admin@123

# # ============================================
# # RBAC Configuration (Placeholders for future)
# # ============================================
# # Role hierarchy: SUPER_ADMIN > ADMIN > DEPT_HEAD > STAFF > CITIZEN
# # app.rbac.role-hierarchy=SUPER_ADMIN > ADMIN > DEPT_HEAD > STAFF > CITIZEN

# # ============================================
# # Security Configuration
# # ============================================
# # BCrypt strength (default: 12)
# # app.security.bcrypt-strength=12